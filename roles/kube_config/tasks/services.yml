- name: nginx service
  k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: nginx-deployment
        namespace: temp
      spec:
        type: NodePort
        selector:
          app: nginx
        ports:
          - protocol: TCP
            port: 80
            targetPort: 80
            nodePort: 30080


